# Thread identifiers and the random number generator are thread-local. Enabled per board rather
# than globally, since the native simulator gets its own from the host toolchain.
CONFIG_THREAD_LOCAL_STORAGE=y
